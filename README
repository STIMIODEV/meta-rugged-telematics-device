This README file contains information on the contents of the meta-telematics-device-source layer.

[Procedure to add the Yocto layer]
----------------------------------
	* Replace the "meta-telematics-device" layer with "meta-telematics-device-source" layer in the "bblayers.conf" file.
	* Compile the core-image-minimal with below command:
		$ bitbake core-image-minimal
